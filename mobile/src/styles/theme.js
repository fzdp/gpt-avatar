import { StyleSheet } from 'react-native';

export const theme = StyleSheet.create({
  appTitle: {
    fontSize: 48,
    color: 'white',
    marginBottom: 48,
  },
  btnFullWidth: {
    width: '100%',
    height: 50,
    backgroundColor: '#32CD32',
    borderRadius: 5,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export const primaryColor = '#a224f9';
